{
  "name": "@funnel/journey-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --env local --outdir dist",
    "test": "vitest run src/consumer.test.ts src/envelope.test.ts src/funnel-trigger.test.ts",
    "test:integration": "vitest run src/integration.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@funnel/clickhouse": "workspace:*",
    "@funnel/event-contracts": "workspace:*",
    "@funnel/journey-engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "4.1.11",
    "wrangler": "4.127.1"
  }
}
