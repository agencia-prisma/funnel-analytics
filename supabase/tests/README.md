# Database tests

RLS, constraints and database contracts will be added together with the first business schema in EPIC 01.

The remote project is linked locally with the Supabase CLI. Project references and credentials must never be committed.
